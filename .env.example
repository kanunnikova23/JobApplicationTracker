## .env is a text file where you put environment variables (like DB_URL, SECRET_KEY, etc).
    Your app reads from this.

## 🔧 Setup
    1. Copy `.env.example` to `.env`
    2. Fill in your database URL